using UnityEngine;

// компонент для идентификации игрового объекта в сценарии
public class ScenarioObject : MonoBehaviour
{
    // уникальный идентификатор объекта, настраиваемый в инспекторе
    [SerializeField]
    private string id;

    // публичное свойство для безопасного чтения идентификатора
    public string Id => id;

    // зачем нужен этот скрипт?
    // в крупном проекте будут большие сцены с обилием объектов
    // чтобы конкретизировать "какой именно предмет нужно взять в руки"
    // этот скрипт необходим

    // то есть если по-научному, этот кусок кода идентифицирует конкретный объект на сцене,
    // чтобы сценарий понимал, с каким объектом работает
}